<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
    xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
        http://www.springframework.org/schema/security
        http://www.springframework.org/schema/security/spring-security.xsd
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd">

    <http auto-config="true" use-expressions="true">
        <intercept-url pattern="/resources/**" access="permitAll" />
        <intercept-url pattern="/css/**" access="permitAll" />
        <intercept-url pattern="/auth/login**" access="permitAll" />
        <intercept-url pattern="/auth/signup**" access="permitAll" />
        <intercept-url pattern="/auth/find-id**" access="permitAll" />
        <intercept-url pattern="/auth/find-password**" access="permitAll" />
        <intercept-url pattern="/error/**" access="permitAll" />
        <intercept-url pattern="/**" access="permitAll" />

        <form-login
            login-page="/auth/login"
            login-processing-url="/auth/login"
            username-parameter="username"
            password-parameter="password"
            default-target-url="/main"
            authentication-failure-url="/auth/login?error=true" />

        <logout
            logout-url="/auth/logout"
            logout-success-url="/auth/login?logout=true"
            invalidate-session="true"
            delete-cookies="JSESSIONID" />

        <remember-me
            key="hifive-remember-key-2026"
            remember-me-parameter="remember-me"
            token-validity-seconds="2592000"
            user-service-ref="customUserDetailsService" />
    </http>

    <authentication-manager>
        <authentication-provider user-service-ref="customUserDetailsService">
            <password-encoder ref="passwordEncoder" />
        </authentication-provider>
    </authentication-manager>

    <beans:bean id="passwordEncoder"
        class="com.app.security.CompatiblePasswordEncoder" />

</beans:beans>
