<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC
		   "-//mybatis.org//DTD Mapper 3.0//EN" 
		   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="faq_mapper">

	<select id="selectAll" resultType="com.app.dto.FaqDTO">
		SELECT * FROM faq ORDER BY order_no ASC, faq_no DESC
	</select>

	<select id="selectOne" resultType="com.app.dto.FaqDTO">
		SELECT * FROM faq WHERE faq_no = #{faqNo}
	</select>

	<insert id="insert">
		INSERT INTO faq (category, question, answer, order_no)
		VALUES (#{category}, #{question}, #{answer}, #{orderNo})
	</insert>

	<update id="update">
		UPDATE faq
		SET category=#{category}, question=#{question}, answer=#{answer},
		order_no=#{orderNo}
		WHERE faq_no=#{faqNo}
	</update>

	<delete id="delete">
		DELETE FROM faq WHERE faq_no=#{faqNo}
	</delete>

</mapper>